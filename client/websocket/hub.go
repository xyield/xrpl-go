package websocket

// type responseChan chan []byte

// type hub struct {
// 	// Registered connections.
// 	connections map[string]responseChan
// }

// func (h *hub) registerConnection(id string, c responseChan) {
// 	h.connections[id] = c
// }

// func (h *hub) unregisterConnection(id string) {
// 	delete(h.connections, id)
// }
